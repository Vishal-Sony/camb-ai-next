export const metadata = {
  title: 'Sound Sculptor',
  description: 'Camb AI Assignment',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
